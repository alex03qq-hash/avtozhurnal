# Порт HTTP-сервера (в проде он же отдаёт собранный интерфейс)
PORT=4000

# Каталог с файлом базы данных приложения (db.json создаётся автоматически)
DATA_DIR=./data

# Режим работы: development | production
NODE_ENV=development
